# sts2-spire1 夜间工作方案（2026-08-25 00:23 起）

> 常设条款：每次会话开始先 `date` 校准时间感；游戏进程存活时禁止部署（dll/pck 锁）；
> 联机测试期间不碰 `mods/` 目录，产物一律走 `dist/stage`；并发 subagent ≤2。

## 0. 时间基线

- 现在时刻：2026-08-25 00:23 (+0800, 周二)
- 上一批已推送：`8781855`（池修复）/ `c6ab934`（裂变+力量表）/ `8cce489`（消耗修复）
- 本方案批准前**不动代码**。

## 1. P0 — 阻断联机的修复与工具

### 1.1 遗传算法池归属修正（用户实锤）
- 现状：`GeneticAlgorithm.cs` 无 `[Pool]` → 继承基类 `Spire1CardPool` = **红色铁甲池**。
  jar 权威：`cards/blue/GeneticAlgo.class` ⇒ 应属蓝色机器人池。
- 动作：加 `[Pool(typeof(DefectCardPool))]`；顺带用 jar 核对其费用/数值/升级
  （一代：1 费，获得 1(2) 敏捷，永久 +1 成长）。
- 已重写的 GA 文件含诊断日志与一代语义，保留。
- **验证**：构建 + 下局日志确认 GA 只出现在机器人奖励。

### 1.2 跳过节点工具（用户点名开发）
- 目标：火堆等房间进入黑屏死循环时，一键跳过当前标定节点。
- 方案（用户指定 GUI 化）：在地图页面直接提供"跳过当前节点"入口。
  - UI：NMapScreen 注入一个跳过按钮（选中节点后可用；样式随地图皮肤），
    点击=将标定节点标记为完成并回到选点状态。
  - 需研究的 API：RunState 当前房间完成标记 / NMapScreen 选点推进 /
    RoomHandler 正常收尾路径（参照 ShopRoomHandler 收尾三连）。
  - 联机语义：必须走网络化包装（参照 ConsoleCmdGameAction），否则单端跳过=新失同步源。
  - 兜底保留控制台命令 spire1-skip 同一实现（GUI 按钮内部调用同一路径）。
- **验证**：双人局人为复现火堆黑屏后 `spire1-skip` 双端一致进入下一节点选点。

### 1.3 火堆黑屏根因深挖（用户确认机制：进火堆必黑屏；杀进程从火堆重启则跳过入场所以正常）
- 日志事实：两次卡死均为"入队动作永不执行"；`win`/`block` 是自救不是病因；
  进房加载链无我方代码（RestSiteBackgroundPatch 仅作用于 Spire1Act，已排除）。
- 待查方向（按嫌疑，用户裁定 SpeedX 在正常对局从无问题——已从嫌疑剔除）：
  ① Act4Heart 的 NRestSiteButton overlay hook（火堆按钮创建后注入）；
  ② Watcher(ws) 注册的 rest_site 场景在多人下的加载；
  ③ AFTP-MP-Balance 对房间表的改动；④ 本体公测版。
- 若锁定第三方：输出兼容护栏或向其作者反馈（附 RitsuLib divergence zip 路径清单）。

### 1.4 MP 失同步三案收口
- 粘液(SLIMED) 抽牌堆移位 / Sunder Canceled / 黑屏前 win：统一走
  "最小 mod 集(BaseLib+Spire1+RitsuLib) → 二分加回"实验流程；
  复现即上游 bug（带 RitsuLib divergence zip 上报）；消失即按二分结果出兼容层。

## 2. P1 — 内容完整性

### 2.1 一代游玩元素知识库（subagent 任务）
- 派 research 型 agent 以 desktop-1.0.jar 反编译 + 官方 wiki 交叉，
  产出 `research/sts1-kb/`：
  - `cards-{red,green,blue,colorless}.json`（费用/类型/稀有度/效果文本/升级差/关键词）
  - `relics.json` / `potions.json` / `events.json`
  - 字段以 jar 字节码为权威，wiki 仅作交叉校验并在条目标注来源等级。
- **本地化原文为重点**：每张卡/遗物/药水/事件条目附官方 EN 原文全文，
  并尽力收集一代官方简中文本一并入库（标注来源与置信度）；
  本项目 zhs 表与官方原文逐条可 diff —— 翻译审校、缺键检测、措辞仲裁都以此为准。
- 用途：移植数值仲裁、loc 审校、覆盖缺失比对均以 KB 为单一事实源。

### 2.2 遗留池复核尾巴
- `Thunderclap` 在 jar 清单中缺失（grep 大小写问题待核），确认其归属与注入状态。

## 3. P1 — 工程债与方法论沉淀

- **技能化**：建 skill《sts2-spire1 卡牌一致性审计》五步法——
  ①loc↔实现双向扫描（占位符/关键词/动作动词）②jar 字节码仲裁
  ③引擎源码链路验证（Clone/SavedProperty/Keywords 缓存等已踩点）④运行时探针日志
  ⑤RitsuLib 转储对拍。附引擎速查表（LocalKeywords 缓存、CanonicalKeywords 必须 public、
  PowerLoc 键格式 SPIRE1-SLUG.title/description/smartDescription、AddModelToPool 冻结、
  Slugify 特例 CreativeAI→CREATIVE_AI 等）。
- CodeOpt 性能流（跑完需 autoslay 回归）。
- 覆盖 drain 尾巴：自然局消化剩余缺口；PoolCensus 日志核对三池终态。

## 4. 执行顺序

1. §1.1（5 分钟，含构建）
2. §1.2 研究 API → 实现 → 单人冒烟（控制台自测）→ 构建暂存
3. §2.1 派发 subagent（后台，≤2 并发）
4. §1.3 日志深挖（可与 3 并行）
5. §3 技能化落盘
6. 用户允许部署窗口：dll+pck 重打 → 同步本地 mods → 三 zip 刷新 → 哈希校验
7. 全程每步落盘 DEVLOG，节点完成即 commit push

## 5. 验证标准（Definition of Done）

- §1.1/§1.2/§2.x：构建零错误 + 对应行为日志实证（非"应该好了"）
- §1.2 额外：双端状态一致（无新增失同步）
- 所有变更 commit+push，dist 三包哈希与 live 一致
