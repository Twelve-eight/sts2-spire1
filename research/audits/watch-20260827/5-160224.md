# 分歧简报 #5 — DUPLICATOR 事件：平衡补丁双端不同步（KNEEL vs LEAVE）

- **时间**: 2026-08-27 16:02:24（新一局，Host=储君 76561199033460852 ↔ Remote 76561199466878739）
- **zip**: ritsulib_state_divergence_20260827_160224_..._checksum_55_....zip → G:/tmp/watch-div-5/
- **触发动作**: `Exiting event room EVENT.ACTSFROMTHEPAST-DUPLICATOR`（复制器事件）
- **Checksum**: #55, local=2016487423 vs remote=3925341186
- **结果**: 断线 `Disconnecting peer, reason: StateDivergence`

## 真实差异（dump 逐行 diff）
仅 2 行：
```
Relic BOOK_OF_FIVE_RINGS Props: CardsAdded=4 (local) vs CardsAdded=2 (remote)  floor=19
RNG counter Niche: Counter 57 (local) vs 53 (remote)
```

## 决定性证据：两端事件选项不同（local/remote debug log）
对玩家 76561199033460852 的 DUPLICATOR 事件：
- **Host 端**选项: `PRAY`(祈祷) / `KNEEL`(跪下: 失去生命, 复制2张随机升级卡) — 页面 `INITIAL_REBALANCED`
- **Remote 端**选项: `PRAY`(祈祷) / `LEAVE`(离开) — 页面 `INITIAL`（原版）

该玩家在两端都按了**选项索引 1**：
- Host 端执行 `INITIAL_REBALANCED.options.KNEEL` → 复制2张卡 → CardsAdded 2→4，Niche RNG 多耗 4 次
- Remote 端执行 `INITIAL.options.LEAVE` → 什么都不做

两端 AFTP 主 mod（ActsFromThePast 1.0.5）和 **ActsFromThePastMultiplayerBalance 0.0.1** 版本完全一致，但再平衡补丁只在 Host 端生效。

## 根因
**ActsFromThePastMultiplayerBalance（AFTP 联机平衡补丁 v0.0.1）的事件页面替换补丁在客户端（Remote）未生效**——大概率是该 mod 的 Harmony 补丁只在主机侧条件下应用（或客户端补丁加载/应用失败但无报错）。同一选项索引映射到不同动作 → 状态必然分歧 → 断线。

## 与已知问题的关系/重要修正
- **简报 #2（SHINING_LIGHT，Niche 32 vs 28，同样差 4 次）强烈疑似同根因**：平衡补丁改变了事件选项/效果，只在 Host 端执行额外的 RNG 消费。建议把 #2 的主嫌从"AFTP 事件 RNG 不对称"修正为"ActsFromThePastMultiplayerBalance 补丁单端生效"。
- Slimed 系（#1/#3）与 DustyTome/DARV（#4）仍是独立问题，但 AFTP 家族的联机问题至此有三种形态：
  1. ClassicSlimed 标记丢失（已知）
  2. MultiplayerBalance 补丁单端生效（本次新证据，DUPLICATOR，可能也包括 SHINING_LIGHT）
  3. DustyTome × DARV 奖励错位（已知）

## 建议（供参考，未改代码）
联机测试期间两端**同时禁用 ActsFromThePastMultiplayerBalance**，即可验证并规避形态 2。
